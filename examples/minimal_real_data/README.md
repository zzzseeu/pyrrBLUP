# Minimal Real Data Example

This example is a deterministic subset of the real `loc_BeiJ` data:

- `32` samples
- `64` markers

Files:

- `genotype_012.csv`
- `phenotype.csv`
- `metadata.json`
- `run_example.py`

Run it with:

```bash
python examples/minimal_real_data/run_example.py
```
